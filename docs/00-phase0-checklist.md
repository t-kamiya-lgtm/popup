# 00. Phase 0 調査手順書（実装着手前にやること）

開発を始める前に調べてほしいことがあります。
専門知識は不要です。ブラウザとスマレジ・リピートの管理画面があればできます。

| 調査 | 内容 | 状態 | 所要 |
| --- | --- | --- | --- |
| **A** | カート〜注文完了ページの**アドレス（URL）** | ✅ **確認済み**（下記参照） | — |
| **C** | 商品ページの **URL と商品の識別方法** | ✅ **確認済み・方針確定**（下記参照） | — |
| **D** | スマレジEC **受注API の仕様** | ✅ **確認済み**（下記参照） | — |
| **E** | サンクスページの**差し込みタグ一覧** | ✅ **確認済み・連携方式を最終確定**（下記参照） | — |
| **F** | 受注APIの**アクセストークンの取得方法** | ✅ **確認済み・判断が必要**（下記参照） | — |
| **B** | 突合キーの確認、または初回/継続判定の実装要否のご判断 | 🔲 **ご判断をお願いします** | 3 分 |

## ✅ 調査Aの結果（確認済み）

実際に購入手続きを進めていただいた結果、**全ステップが `www.primedirect.jp` の
同一アドレスでした。** これはいちばん良いパターンです。

- 「バナーを見ただけで、後から買った人」まで含めて成果を計測できます
- 追加の複雑な仕組み（別アドレス間で情報を橋渡しする処理）が不要になり、
  実装もシンプルになります

## ✅ 調査Cの結果（確認済み・方針転換）

頂いた 2 つの商品ページ URL について、ご回答いただきました。

```
https://www.primedirect.jp/products/pm100/   ← pm100 は商品コードではない
https://www.primedirect.jp/protein.html      ← LP（複数商品の紹介ページ）
```

この結果を受けて、**商品の識別方法を変更しました。**

> ❌ 商品ページに商品コードタグを設置し、そのページ ＝ その商品とみなす
> ✅ サンクスページで受け取る「**実際にカートに入った商品コード**」だけで商品を識別する

これにより、**商品ページに新しいタグを追加設置する必要がなくなりました。**
設置作業はサンクスページの CV タグ 1 箇所に集約されます。

## ✅ 調査Dの結果（確認済み・さらに良い方法に変更）

いただいた「スマレジEC・受注API」の仕様書を確認しました。
これは注文データを**プログラムから直接取得できる仕組み**（管理画面を人が見るのではなく、
システム同士が自動でやり取りする窓口）です。この中の
「ご購入明細商品コード」という項目が、まさに探していた
「実際にカートに入った商品コード」そのものでした。しかも**注文の明細（商品）ごとに**
正確に取れる形式だったため、**サンクスページの差し込み変数から商品コードを
読み取る方式より、さらに正確な方法に変更しました。**

**その後、サンクスページの差し込みタグ一覧を実際にいただき、さらに更新しました。**

## ✅ 調査Eの結果（確認済み・連携方式を最終確定）

いただいたタグ一覧の中に、`{商品毎出力}` と `{/商品毎出力}` という**繰り返しタグ**が
ありました。これで囲んだ部分は「カートの中の商品の数だけ」自動的に繰り返されます。
つまり、**受注APIを待たなくても、注文完了画面が表示された瞬間に、
商品コード・数量・金額を（複数商品でも）正確に取得できる**ことが分かりました。

**最終的にこうなりました:**

- サンクスページのタグは、**注文番号・商品コード・数量・金額をその場で送る**形になりました
  （受注APIの応答を待つ必要がなくなり、より速く・より確実になりました）
- 受注APIは、**「初回購入か、2回目以降か」の判定 1 点だけ**に使います。
  差し込みタグの一覧に、この区別を示すものが見当たらなかったためです
- 1 回の注文で複数の商品を買った場合にも、**最初から正しく対応**できます

## ✅ 調査Fの結果（確認済み・判断が必要）

「アクセストークンをどこで発行するか」を教えていただいたところ、
想像していた「発行ボタンを押すだけ」の仕組みではなく、
**OAuth（オーオース）と呼ばれる、少し手間のかかる仕組み**でした。

具体的には、次のような複数ステップの手続きです。

1. 御社の管理画面で、こちらのツール用の「アプリ」を 1 回登録する
2. 認証ページで「このツールにデータへのアクセスを許可します」と承認する
3. 発行された鍵は **100日で失効する**ため、定期的に自動更新する仕組みを
   こちら側で継続的に維持する必要がある

これは「作って終わり」ではなく、**その後もずっとメンテナンスが要る仕組み**です。
そして、この仕組みが叶えるのは**「初回購入か、2回目以降か」という 1 つの区別だけ**です。

### ご判断をお願いしたいこと

この 1 つの区別のために、上記の仕組みを作る価値があるかどうかです。
選択肢は次の3つです。

| 案 | 内容 | おすすめ度 |
| --- | --- | --- |
| **① もう一度だけ確認する** | 「初回購入」「新規」等の名前で、差し込みタグの一覧に
  同じような項目が実はもう1つあるかもしれません。それが見つかれば、
  この面倒な仕組み自体が丸ごと不要になります | ★★★ まず試す価値あり |
| **② 今は実装しない** | 最初のリリースでは「初回か2回目以降か」を区別せず、
  全ての購入をまとめて集計します。後から必要になったタイミングで
  追加で作ることもできます | ★★ 早く使い始めたい場合 |
| **③ 今回実装する** | 手間はかかりますが、最初から正確に区別できます | ★ 区別が重要な場合 |

**特にこだわりがなければ、①を試していただき、見つからなければ②で進めることを
おすすめします。** 開発の着手を遅らせずに済みます。

**残るのは調査 B のみ**で、これは後日で構いません。
**開発（Phase 1）にはすでに着手できる状態**です。

---

**この文書の 6 章に、そのままコピーして埋められる記入シート**があります。
調査 B が揃い次第、埋めて返信いただければ実装に反映します。

---

## 調査 A: カート〜注文完了ページの URL を調べる（完了・参考情報）

> ✅ この調査はすでに完了しています。以下は「なぜこれを聞いていたか」の
> 背景説明です。同様の調査を別サイトで行う際にも使えます。

### なぜ調べるのか

ブラウザには「**別のアドレスのページとは情報を共有できない**」という
セキュリティ上の決まりがあります。

```
www.primedirect.jp でバナーを見た人の情報
        ↓
   別のアドレス（例: cart.example.com）の注文完了ページ
        ↓
   「この人はバナーを見た人だ」と分からない ＝ 成果を計測できない
```

購入手続きの途中でアドレスが変わるかどうかで、**計測方法を変える必要がある**ため、
最初に確認します。変わっていても対応できますが、**作り方が変わります**。

### 手順

> ⚠️ 実際に注文を完了させる必要はありません。**手順 5 の手前まで**で
> ほとんどの情報が取れます（注文完了ページだけは 3.5 章の方法で確認します）。

#### 1. サイトを開く

パソコンのブラウザ（Chrome / Edge / Safari など）で
`https://www.primedirect.jp/` を開きます。

#### 2. 商品をカートに入れる

適当な商品を 1 つ選び、「カゴに入れる」を押します。

#### 3. 各画面で「アドレスバー」を見る

ブラウザの**いちばん上にある、アドレスが表示されている欄**です。

```
┌────────────────────────────────────────────┐
│  ←  →  ⟳   https://www.primedirect.jp/cart/ │  ← ここ
└────────────────────────────────────────────┘
```

**このアドレスを全部コピーして、メモ帳などに貼り付けてください。**

コピーの方法: アドレスバーを 1 回クリック → 全体が選択される →
`Ctrl + C`（Mac は `⌘ + C`）

#### 4. 次の画面に進むたびに、同じことを繰り返す

以下の各画面で、**そのつどアドレスをコピー**してメモしてください。

| # | 画面 | 画面の見た目の目印 |
| --- | --- | --- |
| ① | **カート画面** | 「ショッピングカート」「カートの中身」など |
| ② | **お客様情報の入力画面** | 氏名・住所・電話番号を入れる画面 |
| ③ | **お支払い方法の選択画面** | クレジットカード / 代引き などを選ぶ画面 |
| ④ | **注文内容の確認画面** | 「この内容で注文する」ボタンがある画面 |
| ⑤ | **ご注文完了画面** | 「ご注文ありがとうございました」の画面 |

> ④ の「注文する」ボタンは、押すと実際に注文が確定します。
> **⑤ を確認する方法は次の 3.5 章**を読んでから進めてください。

### 3.5 注文完了画面（⑤）の確認方法

⑤ だけは実際に注文しないと表示されません。次のいずれかで確認してください。

| 方法 | やり方 | 備考 |
| --- | --- | --- |
| **A. テスト注文機能を使う**（推奨） | スマレジ・リピートに「テスト注文」「テストモード」の機能があれば、それを使う | 課金なし。機能の有無は 4 章の質問文でサポートに確認できます |
| **B. 少額商品を実際に買う** | 最も安い商品を 1 つ、代金引換で注文 → 完了画面の URL をコピー → **すぐに電話でキャンセル** | 確実。社内の方が実施すれば問題になりません |
| **C. 過去の注文完了メールを見る** | 完了メールに完了ページへのリンクがあれば、そのアドレスを見る | 取れないことも多い |

**注文完了画面のアドレスが最重要**です。ここが計測の要になります。

### 判定（この結果で作り方が決まります）

メモした ①〜⑤ のアドレスを見比べてください。
見るのは `https://` の直後から、次の `/` までの部分です。

```
https://www.primedirect.jp/cart/
        └─────┬─────┘
          ここが「ホスト名」
```

| ①〜⑤ の結果 | 判定 | 意味 |
| --- | --- | --- |
| **全部 `www.primedirect.jp`** | 🟢 **パターンA** | 計測精度がいちばん高い構成。実装もシンプル |
| **途中から違う名前に変わる**<br>（例 `secure.〇〇.jp`、`〇〇.smaregi.jp` など） | 🟡 **パターンB** | 対応可能。ただし**「バナーを見ただけで買った人」の計測ができなくなります** |

#### 🟡 パターンB だった場合の追加確認（重要）

別のアドレスになっていた場合、**そのカート側に独自のアドレスを設定できないか**を
スマレジ・リピートのサポートに確認してください（4 章に質問文があります）。

例: `secure.smaregi-repeat.jp` → `cart.primedirect.jp` に変更できるか

**変更できれば パターンA と同じ精度になり、実装も簡単になります。**
確認する価値は十分にあります。

### スマートフォンでも同じ確認を

スマホ版で URL が違う場合があります（例: `sp.` が付く、`/sp/` が入る）。
スマホでも同じ手順で ①〜④ のアドレスを確認してください。

スマホでのアドレスの見方: 画面上部のアドレスバーをタップ → 長押し → 「コピー」

---

## 調査 B: 初回/継続判定の実装方針（ご判断をお願いします）

> 「✅ 調査Fの結果」（本文書の冒頭）のとおり、アクセストークンの取得は
> 一度きりの単純な発行ではなく、**継続的なメンテナンスが要る仕組み（OAuth）**
> だと分かりました。この仕組みは「初回購入か、2回目以降か」の区別だけに使うため、
> **まずは、その区別自体が本当に必要かをご確認いただくのが最優先**です。

### お願いしたいこと（優先順）

1. **サンクスページの差し込みタグ一覧を、もう一度見ていただき、
   「初回購入」「新規」「リピート」等の名前のタグが無いか確認**してください。
   見つかれば、OAuthの仕組みを作らずに済みます（いちばん簡単な解決策です）。
2. 見つからなければ、以下のいずれかをお選びください。
   - **今は実装しない**（全ての購入をまとめて集計し、開発を先に進める）
   - **実装する**（OAuth連携の手間を受け入れて、初回/継続を正確に区別する）

実装する場合は、追加で以下もご確認をお願いします。

| # | 項目 | 用途 |
| --- | --- | --- |
| 1 | サンクスページの `{注文番号}` タグの値（例 `165`）が受注APIのどの項目と一致するか | 受注API側でその注文を検索するためのキー |
| 2 | `order.order_cnt`（購入回数）は「その顧客の通算注文回数」で合っているか | 初回注文かどうかの判定に使う |

### そのまま送れる質問文

```
お世話になっております。プライムダイレクトの〇〇です。

アクセストークンの取得方法（OAuth）についてご案内いただき
ありがとうございました。

1点確認させてください。差し込みタグ一覧の中に、
「初回購入か、2回目以降（リピート）か」を区別できるタグは
ございますでしょうか（先にいただいた一覧には見当たりませんでした）。
もしあれば、そちらの利用を検討したいと考えております。

もし無ければ、以下2点もあわせてご教示いただけますでしょうか。

1. サンクスページの {注文番号} タグで出力される値（例: 165）は、
   受注APIの order.order_id（受注ID）と order.ec_order_id（EC注文番号）の
   どちらと一致しますか。

2. order.order_cnt（購入回数）は、その顧客の通算注文回数という
   理解で合っておりますでしょうか。この値が 1 の注文を
   「初回注文」として扱う想定です。

以上、よろしくお願いいたします。
```

---

## 調査 C: 商品ページの URL と商品識別方法（完了・方針転換済み）

頂いた 2 件と、その後のご回答を反映しました。

```
https://www.primedirect.jp/products/pm100/   ← pm100 は商品コードではないとのこと
https://www.primedirect.jp/protein.html      ← LP（複数商品の紹介ページ）とのこと
```

### 分かったこと

商品ページの URL からは商品を正しく特定できないことが分かりました。
そのため、**商品ページに新しいタグを追加する方式はやめました。**

代わりに、**注文完了（サンクス）ページで受け取る「実際にカートに入った商品コード」**
だけを商品の正として扱う方針にしました（その後、調査 D の結果によりさらに、
受注APIから直接取得する形に確定しています）。

### この方針転換で何が変わったか

- 商品ページへのタグ設置作業が**不要**になりました
- 「どのページを見たか」（imp・クリック数）と「何が売れたか」（商品別の実績）は、
  別々の表としてレポートに出ます（同じ表では混ぜません）
- ポップアップを「特定の商品ページにだけ出す」設定は、
  URL のアドレスのパターン（例: `/products/` で始まるページすべて）で行います

---

## 調査 D: スマレジEC 受注APIの仕様（完了）

いただいた受注APIの仕様書を確認しました。当初はこれを CV 連携の主経路にする
想定でしたが、その後いただいた差し込みタグ一覧（調査E）により、
商品コード・金額はページタグから直接取得できると分かったため、
**受注APIの役割は「初回/継続の判定」1点に縮小**しています。

## 調査 E: サンクスページの差し込みタグ一覧（完了・連携方式を最終確定）

「✅ 調査Eの結果」（本文書の冒頭）のとおり、`{商品毎出力}{/商品毎出力}` という
繰り返しタグにより、商品コード・数量・金額を注文完了画面から直接、
複数商品ぶんまとめて取得できることが分かりました。これにより
サンクスページのタグは「注文番号・商品コード・数量・金額を直接送る」形に確定し、
受注APIは初回/継続判定のためだけに使う設計になっています。

技術的な詳細は、本プロジェクトの設計書 `docs/09-cart-integration.md` の 3〜4 章に
まとめています。

---

## 4. まとめ: やることリスト

- [x] **A-1〜A-4** URL 確認 → 完了（全ステップ同一アドレスと判明）
- [x] **C-1〜C-2** 商品ページ URL 確認・商品識別方針を確定
      （pm100 は商品コードでない、protein.html は LP と判明）
- [x] **D-1** 受注APIの仕様確認
- [x] **E-1** 差し込みタグ一覧を確認 → CV連携方式を最終確定
      （商品コード・金額はページタグから即時取得。受注APIは初回/継続判定のみに使用）
- [x] **F-1** アクセストークンの取得方法を確認 → OAuth（継続メンテナンスが必要）と判明
- [ ] **B-1**（最優先）「初回/継続」を示す差し込みタグが他に無いか再確認
- [ ] **B-2** 無ければ「今は実装しない」か「実装する」かをご判断
- [ ] **B-3**（実装する場合のみ）`{注文番号}` の突合先・`order_cnt` の意味を確認

残りは **B（初回/継続判定の方針）のみ**です。この判断を待たずに開発（Phase 1）は進められます。

---

## 5. FAQ

**Q. 実際に注文してしまって大丈夫ですか？**
A. 自社サイトなので問題ありません。代金引換で注文し、完了画面の URL を控えたあと
電話でキャンセルすれば費用も発生しません。テスト注文機能があればそちらが確実です。

**Q. 全部揃わないと開発を始められませんか？**
A. いいえ。**調査 A・C・D・E・F は完了しており、着手できる状態です。**
残る調査 B（初回/継続判定を実装するかどうかのご判断）は開発と並行で構いません。

**Q. パターンB（別ドメイン）だと何ができなくなりますか？**
A. 「バナーを**クリックして**買った人」は計測できます。
「バナーを**見ただけ**（クリックせず）で、後から買った人」が計測できなくなります。
その分は、一部の人にわざとバナーを出さない「比較用グループ」を作って、
購入率の差から全体の効果を測ります（この仕組みは最初から組み込みます）。

**Q. 管理画面のメニュー名がこの文書と違います**
A. システムのバージョンで名称が変わります。3 章のキーワードで探して、
見つからなければサポートへの質問文をそのままお使いください。

**Q. 途中で分からなくなりました**
A. **その画面のスクリーンショットを送ってください。**
どこを見ればよいか、こちらから具体的にお伝えします。

---

## 6. 記入シート（コピーして埋めてください）

```
■ 調査A: URL（パソコン）─ ✅ 確認済み
 ① カート画面          : https://www.primedirect.jp/cart/?product_id=...
 ② お客様情報入力画面   : https://www.primedirect.jp/shopping/
 ③ お支払い方法選択画面 : https://www.primedirect.jp/shopping/payment.php
 ④ 注文確認画面        : https://www.primedirect.jp/shopping/confirm.php
 ⑤ ご注文完了画面      : https://www.primedirect.jp/shopping/complete.php
 判定                  : 🟢 パターンA（全ステップ同一アドレス）

■ 調査A: URL（スマートフォン）─ 任意（PC と同じ構成なら省略可）
 ① カート画面          : 
 ② お客様情報入力画面   : 
 ③ お支払い方法選択画面 : 
 ④ 注文確認画面        : 

■ 調査B: 初回/継続判定の方針（スクリーンショットでも可）
 「初回/継続」を示す他のタグの有無: あり（タグ名: ______）/ なし / 不明
 タグが無かった場合の方針      : 今は実装しない / 実装する

 ─（「実装する」を選んだ場合のみ、以下も）─
 {注文番号}タグの値が一致する項目: order.order_id / order.ec_order_id / 不明
 order_cnt=1が初回注文か        : はい / いいえ / 未確認

■ 調査C: 商品識別方法 ─ ✅ 確認済み（サンクスページのループタグ方式に確定・記入不要）
■ 調査D: 受注API仕様 ─ ✅ 確認済み・記入不要
■ 調査E: 差し込みタグ一覧 ─ ✅ 確認済み・連携方式を最終確定・記入不要
■ 調査F: アクセストークン取得方法 ─ ✅ 確認済み（OAuthと判明）・記入不要

■ その他（気づいたこと・詰まったこと）
 
```
